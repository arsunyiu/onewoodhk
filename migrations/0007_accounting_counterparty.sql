-- ============================================================
-- 一木工程  |  Migration 0007: 會計出入帳新增「收款人/入帳名稱」欄位
-- 讓每筆出入帳可記錄實際收款/付款對象（如員工姓名、供應商、分判商公司名等），
-- 方便日後對帳與報表追蹤資金流向
-- ============================================================

ALTER TABLE accounting_entries ADD COLUMN counterparty_name TEXT;
