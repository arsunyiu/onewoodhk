-- ============================================================
-- 一木工程  |  Migration 0014: 發票新增「共分幾期」欄位
-- 因應發票PDF需清楚顯示本期收款佔總金額百分比及所屬期數（如「訂金 30%，第1期/共2期」），
-- seq 欄位已記錄「這是第幾張發票」，但缺少使用者預計的「總共分幾期」，
-- 新增 installment_total（可選，未填時PDF僅顯示「第X期」不顯示「共Y期」）
-- ============================================================

ALTER TABLE invoices ADD COLUMN installment_total INTEGER;
